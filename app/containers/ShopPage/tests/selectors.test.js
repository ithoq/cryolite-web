// import { fromJS } from 'immutable';
// import { makeSelectShopPageDomain } from '../selectors';

// const selector = makeSelectShopPageDomain();

describe('makeSelectShopPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
